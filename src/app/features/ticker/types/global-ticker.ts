export type GlobalTicker = {
  lastPrice: string;
  tickSize: string;
  prevLastPrice: string;
  priceChange: string;
  priceChangePercent: string;
  lastQuantity: string;
  numberOfTrades: number;
} | null;
